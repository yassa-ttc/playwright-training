const calculateSquare = (source: number): number => source*source;

const calculateTriangleArea = (base: number, height: number): number => base*height/2;

const result = calculateTriangleArea(10, 5);

console.log(result);